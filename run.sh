#!/bin/bash

# --cors Enable CORS via the Access-Control-Allow-Origin header
# (the Cross-Origin Resource Sharing (CORS) mechanism gives web servers cross-domain access 
# controls, which enable secure cross-domain data transfers. 

./http-server/bin/http-server --cors logo/


